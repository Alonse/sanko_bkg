include ./../mixins
doctype html
html
    +head()
    body(class="app")
        //+loader('loader__spinner_falcon')
        +header()
        main.main
            div.gradient-inner
                +feedback-page()
                +hub()
                +footer()
                +compass-button()
