include ./../mixins
doctype html
html
    +head()
    body(class="app")
        //+loader('loader__spinner_falcon')
        +header()
        main.main(id='main')
            div
                +checkout()
                +footer()
                +compass-button()