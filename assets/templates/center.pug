include ./../mixins
doctype html
html
    +head()
    body(class="app")
        //+loader('loader__spinner_falcon')
        +header()
        main.main
            div
                +center-page()
                +center-filter()
                +footer()
                +compass-button()
